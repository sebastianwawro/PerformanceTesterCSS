@model IEnumerable<PerformanceTesterCSS.ViewModels.Performance.ParticipationViewModel>

@{
    ViewData["Title"] = "Participations Index With Nothing";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Participations Index With Nothing</h1>

<p>
    @Html.Raw(ViewData["Info"])
</p>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Season)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConfPart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaperPub)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Season)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ConfPart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaperPub)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
